/**
 * Disclaimer:
 * Secret is just for localhost purpose
 * In a real setup environment secrets shouldn't be committed and should be replaced with environment variables
 */
export const jwtConstants = {
    secret: 'secretKey'
};
